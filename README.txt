"Hellow Github!" 
/*how to clone a project*/
Creat a new Respositories
git clone "ssh URL"

/*how to pull*/

git remote add origin git@github.com:gyd-cmd/testGithub.git
git branch -M main //chosse a  branch you want
git pull // need to pull if there is any updata online
git add "filename"
git commit -m "log"
git push -u origin main // push your updata 
